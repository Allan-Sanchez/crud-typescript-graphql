type User {
  id: ID
  name: String
  email: String
  age: Int
  password: String
}
input UserInput {
  name: String!
  email: String!
  age: Int!
  password: String!
}
type Query {
  helloUser: String
  getUsers: [User]
}

type Mutation {
  newUser(input: UserInput): User!
}
